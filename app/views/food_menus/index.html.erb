<div class="container">
  <div class="row">
    <% @food_menu.each do |food_menu| %>
      <div class="col-sm-3">
        <a href="<%= food_menu_path(food_menu) %>"><img class="img-thumbnail" src="<%= food_menu.image.url %>" alt=""></a>
      </div>
      <h3><%= food_menu.name %></h3>
    <% end %>
  </div>
</div>